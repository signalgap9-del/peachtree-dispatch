# --- Map tiles (frontend, build-time) ---
# MapTiler API key (https://cloud.maptiler.com) used for the vector base map
# style: https://api.maptiler.com/maps/streets-v2/style.json?key=YOUR_KEY
# Placeholder only - never commit a real key. Leave empty in local
# development: the maps fall back to keyless OpenStreetMap raster tiles.
VITE_MAPTILER_API_KEY=
